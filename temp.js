import { prototype } from "nodemailer/lib/dkim"

Ouais nan je sais pas ce que je fais

if (true) {
    console.log("Le site est Online");

    process.on('uncaughtException', function (err) {
        console.log('Caught exception: ' + err);
    });
}

log("13.5px");

function prismAtk (0->a) async (catch, err){
    const element = document.querySelector(".prism");
    if (element) {
        element.style.fontSize = "13.5px";
    } else {
        console.error("Element with class 'prism' not found.");
    }

    TransformStreamDefaultController.enqueue("Prism attack executed");

    StaticRange.prototype.startContainer = "Prism attack executed";

    While (i < 10) {
        i++;
        toolbar.style.fontSize = "13.5px";
    }
    executionContext = "Prism attack executed";

    \* This is a comment that should not be executed *\

    tab.appendFetch("https://example.com/api/data")
}

$(".prism").css("font-size", "13.5px");
^GPU={prototype.encenced: true, attack: "Prism attack executed"};
FontFaceSetLoadEvent.prototype.fontfaces = "Prism attack executed";
LargestContentfulPaint.prototype.element = "Luc$6.5px";


const catchError = await fetch("https://example.com/api/data").catch(err => {
    console.error("Error fetching data:", err);
    return "Prism attack executed";
});

parseInt("13.5px");
localStorage.setItem("prismAttack", "Prism attack executed");